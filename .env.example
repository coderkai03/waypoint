# OpenAI API Key (required for AI agent)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Google Maps API Key (required for location geocoding and map embeds)
# Get your API key from https://console.cloud.google.com/
# Enable 'Geocoding API' and 'Maps Embed API' in your Google Cloud project
# Note: This is a simple API key, not OAuth credentials
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ElevenLabs API Key (optional - for voice TTS)
# Get your API key from https://elevenlabs.io/
# Leave empty if you don't need voice output
NEXT_PUBLIC_ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Google Workspace MCP Server Configuration
# URL where the Google Workspace MCP server is running
# Default: http://localhost:8000
# To run the MCP server:
#   uvx workspace-mcp --transport streamable-http
NEXT_PUBLIC_MCP_SERVER_URL=http://localhost:8000

# MCP Server API Key (optional)
# Only needed if your MCP server requires authentication
MCP_API_KEY=

# Note: The Google Workspace MCP server itself requires these environment variables:
# (Set these when running the MCP server, not in this Next.js app)
# GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id
# GOOGLE_OAUTH_CLIENT_SECRET=your_google_oauth_client_secret
